import React from 'react';

const RecommUser = props => {
  return (
    <div>
      <div>
        {userdata.typeofatrocity.map(atrocity =>
          // <RecommItem
          //   key={comment._id}
          //   comment={comment}
          //   userdataId={userdata._id}
          // />
          {
            if (2 > 1) {
            }
          }, atrocity.value)}
      </div>
    </div>
  );
};

export default RecommUser;
